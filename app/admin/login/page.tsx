import { LockKeyhole } from "lucide-react";import { Brand } from "@/components/brand";import styles from "./login.module.css";export default async function Login({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){const query=await searchParams;return <main className={styles.page}><div className={styles.brand}><Brand dark/></div><form className={styles.form} action="/api/admin/auth/login" method="post"><div className={styles.icon}><LockKeyhole/></div><span>Protected workspace</span><h1>Welcome back.</h1><p>Sign in to manage dramas, preview episodes and tracking destinations.</p><input type="hidden" name="next" value={query.next||"/admin"}/><label><span>Admin password</span><input name="password" type="password" autoComplete="current-password" required autoFocus/></label>{query.error&&<div className={styles.error}>The password is incorrect.</div>}<button>Sign in</button><small>Local development authentication. Replace with Supabase Auth before production.</small></form></main>}
