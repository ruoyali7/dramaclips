import { AdminShell } from "@/components/admin/admin-shell";const names:Record<string,string>={destinations:"Destinations",landings:"Landings",conversions:"Conversions",settings:"Settings"};export default async function Page({params}:{params:Promise<{section:string}>}){const resolved=await params;const name=names[resolved.section]||"Overview";return <AdminShell active={name}><div className="admin-title"><div><p>Workspace</p><h1>{name}</h1></div><button>+ Create</button></div><section className="panel placeholder-panel"><span>MVP workspace</span><h2>{name} management is ready for the Supabase adapter.</h2><p>The public vertical slice and domain rules are implemented. Connect environment credentials to persist and edit live records.</p></section></AdminShell>}
