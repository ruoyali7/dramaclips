import { AdminShell } from "@/components/admin/admin-shell";import { TrackingLinkBuilder } from "@/components/admin/tracking-link-builder";import { getCatalog } from "@/lib/catalog";export const dynamic="force-dynamic";export default async function Page(){const siteUrl=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const {dramas}=await getCatalog();return <AdminShell active="Tracking links"><div className="admin-title"><div><p>Distribution</p><h1>Tracking links</h1></div></div><section className="panel tracking-panel"><span>Social distribution</span><h2>Send every clip to the right drama.</h2><p>Generate a direct watch URL with attribution. CPS destinations remain hidden behind the server-side redirect.</p><TrackingLinkBuilder dramas={dramas} siteUrl={siteUrl}/></section></AdminShell>}
