import { AdminShell } from "@/components/admin/admin-shell";import { PublishCenter } from "@/components/admin/publish-center";import { listVizardSources } from "@/lib/admin/repository";export const dynamic="force-dynamic";export default async function Page(){const sources=await listVizardSources();const metricoolReady=Boolean(process.env.METRICOOL_API_TOKEN&&process.env.METRICOOL_USER_ID&&process.env.METRICOOL_BLOG_ID);return <AdminShell active="Publish Center"><div className="admin-title"><div><p>Final distribution</p><h1>Publish Center</h1></div></div><div className="publish-intro"><b>One publishing path for every finished video.</b><p>Use the original R2 video or upload an edited/Vizard clip, then generate platform hooks, captions, tracking links, and a Metricool-ready package.</p></div><PublishCenter sources={sources} metricoolReady={metricoolReady}/></AdminShell>}
