import Link from "next/link";import { MoreHorizontal,Search } from "lucide-react";import { AdminShell } from "@/components/admin/admin-shell";import { dramas } from "@/lib/demo-data";import { listDramaDrafts } from "@/lib/admin/draft-repository";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{published?:string}>}){const query=await searchParams;const drafts=await listDramaDrafts();return <AdminShell active="Dramas"><div className="admin-title"><div><p>Content library</p><h1>Dramas</h1></div><Link className="admin-primary" href="/admin/dramas/new">+ Add drama</Link></div>{query.published&&<div className="admin-notice">Drama published. It is now available on the public catalog.</div>}{drafts.length>0&&<section className="panel draft-panel"><div className="panel-head"><div><span>Onboarding queue</span><h2>Local encrypted drafts</h2></div></div><table><thead><tr><th>Drama</th><th>Code</th><th>Episodes</th><th>Status</th><th>Action</th></tr></thead><tbody>{drafts.map(d=><tr key={d.id}><td><div><b>{d.title}</b><small>/{d.slug}</small></div></td><td>{d.publicCode}</td><td>{d.episodeCount}</td><td><span className={d.status==="published"?"status":"draft-status"}>● {d.status}</span></td><td>{d.status==="draft"?<form action={`/api/admin/dramas/${d.id}/publish`} method="post"><button className="publish-button">Publish</button></form>:<Link href={`/watch/${d.slug}`} target="_blank">View live ↗</Link>}</td></tr>)}</tbody></table></section>}<section className="panel list-panel"><div className="list-tools"><label><Search/><input placeholder="Search title or code"/></label><select><option>All statuses</option></select></div><table><thead><tr><th>Drama</th><th>Code</th><th>Status</th><th>Route</th><th>Updated</th><th/></tr></thead><tbody>{dramas.map(d=><tr key={d.id}><td><span className="mini-cover" style={{backgroundImage:`url(${d.coverUrl})`}}/><div><b>{d.title}</b><small>{d.tags.join(" · ")}</small></div></td><td>#{d.publicCode}</td><td><span className="status">● Published</span></td><td>/go/{d.routeSlug}</td><td>Aug 11, 2026</td><td><MoreHorizontal/></td></tr>)}</tbody></table></section></AdminShell>}
