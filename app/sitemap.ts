import type { MetadataRoute } from "next";import { dramas } from "@/lib/demo-data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";return[{url:base,changeFrequency:"weekly",priority:1},{url:`${base}/l/featured`,changeFrequency:"daily",priority:.9},...dramas.map(d=>({url:`${base}/watch/${d.slug}`,changeFrequency:"weekly" as const,priority:.8}))]}
