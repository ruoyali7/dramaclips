import { NextRequest,NextResponse } from "next/server";import { publishDramaDraft } from "@/lib/admin/draft-repository";export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;await publishDramaDraft(id);return NextResponse.redirect(new URL("/admin/dramas?published=1",request.url),{status:303})}catch{return NextResponse.json({code:"PUBLISH_FAILED",message:"Draft could not be published"},{status:404})}}
