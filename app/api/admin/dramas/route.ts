import { NextRequest,NextResponse } from "next/server";import { ZodError } from "zod";import { dramaDraftSchema } from "@/lib/admin/drama-schema";import { saveDramaDraft } from "@/lib/admin/repository";export async function POST(request:NextRequest){try{const input=dramaDraftSchema.parse(await request.json());return NextResponse.json({draft:await saveDramaDraft(input)},{status:201})}catch(error){if(error instanceof ZodError)return NextResponse.json({code:"VALIDATION_ERROR",message:"Check the form fields",fieldErrors:error.flatten().fieldErrors},{status:400});console.error("[admin] draft save failed",error instanceof Error?error.message:"unknown");return NextResponse.json({code:"SAVE_FAILED",message:"Could not save the draft"},{status:500})}}
