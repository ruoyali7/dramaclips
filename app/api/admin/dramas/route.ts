import { NextRequest,NextResponse } from "next/server";import { ZodError } from "zod";import { dramaDraftSchema } from "@/lib/admin/drama-schema";import { publishDramaDraft,saveDramaDraft } from "@/lib/admin/repository";export async function POST(request:NextRequest){try{const input=dramaDraftSchema.parse(await request.json());const draft=await saveDramaDraft(input);await publishDramaDraft(draft.id);return NextResponse.json({draft:{...draft,status:"published"}},{status:201})}catch(error){if(error instanceof ZodError)return NextResponse.json({code:"VALIDATION_ERROR",message:"Check the form fields",fieldErrors:error.flatten().fieldErrors},{status:400});console.error("[admin] drama publish failed",error instanceof Error?error.message:"unknown");return NextResponse.json({code:"PUBLISH_FAILED",message:"Could not publish the drama"},{status:500})}}
