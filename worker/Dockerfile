FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg libgl1 libglib2.0-0 nodejs npm && npm install -g @yixiaoermail/cli@3.2.9 && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
RUN useradd -m worker && mkdir -p /work && chown -R worker:worker /work /app
USER worker
ENV HOME=/home/worker WORK_DIR=/work PYTHONUNBUFFERED=1
CMD ["python","-m","hook_worker.main"]
