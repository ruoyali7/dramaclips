import "server-only";import type {PublishAsset,PublishingAccount,PublishingProvider,PublishPostInput,PublishResult,PublishStatus,RemoteMedia} from "../publishing-provider";
export class DisabledDirectProvider implements PublishingProvider{readonly name="direct_unconfigured";async listAccounts():Promise<PublishingAccount[]>{return[]}async uploadMedia(_asset:PublishAsset):Promise<RemoteMedia>{throw new Error("Direct publishing is not certified or configured")}async createPost(_input:PublishPostInput):Promise<PublishResult>{throw new Error("Direct publishing is not certified or configured")}async getStatus():Promise<PublishStatus>{return{status:"failed",safeErrorCategory:"provider_not_configured"}}}
