"use client";
import { Check,Copy,ExternalLink,Link2 } from "lucide-react";
import { useEffect,useState } from "react";
import type { Drama } from "@/lib/types";
type ShortLink={id:string;code:string;dramaSlug:string;source:string;account:string;campaign:string;clip:string;createdAt:string};
export function TrackingLinkBuilder({dramas,siteUrl}:{dramas:Drama[];siteUrl:string}){const [slug,setSlug]=useState(dramas[0]?.slug||"");const [source,setSource]=useState("tiktok");const [account,setAccount]=useState("");const [campaign,setCampaign]=useState("");const [clip,setClip]=useState("");const [created,setCreated]=useState<ShortLink|null>(null);const [recent,setRecent]=useState<ShortLink[]>([]);const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [copied,setCopied]=useState(false);useEffect(()=>{fetch("/api/admin/short-links").then(async response=>response.ok?(await response.json()).links:[]).then(setRecent).catch(()=>{})},[]);const url=created?`${siteUrl}/x/${created.code}`:"";async function create(){if(!clip.trim()){setError("Add a unique Clip ID for this video.");return}setBusy(true);setError("");const response=await fetch("/api/admin/short-links",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({dramaSlug:slug,source,account,campaign,clip})});const json=await response.json();setBusy(false);if(!response.ok){setError(json.message||"Could not create short link");return}setCreated(json.link);setRecent(rows=>[json.link,...rows].slice(0,12))}async function copy(value=url){await navigator.clipboard.writeText(value);setCopied(true);setTimeout(()=>setCopied(false),1600)}return <div className="link-builder"><div className="builder-fields"><label><span>Drama</span><select value={slug} onChange={event=>setSlug(event.target.value)}>{dramas.map(drama=><option value={drama.slug} key={drama.id}>{drama.title} · {drama.publicCode}</option>)}</select></label><label><span>Platform</span><select value={source} onChange={event=>setSource(event.target.value)}><option value="tiktok">TikTok</option><option value="instagram">Instagram</option><option value="youtube">YouTube</option><option value="facebook">Facebook</option><option value="x">X</option></select></label><label><span>Account · optional</span><input value={account} onChange={event=>setAccount(event.target.value)} placeholder="e.g. dramaclips"/></label><label><span>Campaign · optional</span><input value={campaign} onChange={event=>setCampaign(event.target.value)} placeholder="e.g. wolves_launch"/></label><label><span>Clip ID · required</span><input value={clip} onChange={event=>setClip(event.target.value)} placeholder="e.g. airport_hook_01"/></label></div><button className="create-short-link" onClick={()=>void create()} disabled={busy||!slug}><Link2/>{busy?"Generating…":"Generate short link"}</button>{error&&<div className="form-error">{error.includes("short_links")?"Run the new Supabase analytics migration first.":error}</div>}{created&&<div className="generated-link"><span>Short direct-to-drama URL</span><code>{url}</code><div><button onClick={()=>void copy()}>{copied?<Check/>:<Copy/>}{copied?"Copied":"Copy link"}</button><a href={url} target="_blank">Test <ExternalLink/></a></div></div>}<div className="link-note"><b>Everything stays attached to the short code</b><p>Drama, platform, account, campaign, and Clip ID are stored server-side. The public URL stays short.</p></div>{recent.length>0&&<div className="recent-links"><span>Recent links</span>{recent.map(link=><div key={link.id}><code>{siteUrl}/x/{link.code}</code><small>{link.source} · {link.dramaSlug} · {link.clip}</small><button onClick={()=>void copy(`${siteUrl}/x/${link.code}`)}><Copy/></button></div>)}</div>}</div>}
