import { afterEach,beforeEach,describe,expect,it } from "vitest";import { encryptSensitive } from "@/lib/admin/encryption";import { dramaDraftSchema } from "@/lib/admin/drama-schema";
describe("admin content security",()=>{const previous=process.env.CPS_URL_ENCRYPTION_KEY;beforeEach(()=>{process.env.CPS_URL_ENCRYPTION_KEY="1".repeat(64)});afterEach(()=>{process.env.CPS_URL_ENCRYPTION_KEY=previous});it("encrypts destinations without retaining plaintext",()=>{const raw="https://reelslink.com/cps/example";const encrypted=encryptSensitive(raw);expect(encrypted).toMatch(/^v1\./);expect(encrypted).not.toContain("reelslink");expect(encrypted).not.toContain("example")});it("accepts authorized HTTPS ReelShort destinations",()=>{const parsed=dramaDraftSchema.parse({title:"Test Drama",slug:"test-drama",publicCode:"1234",promoCode:"1234",language:"en",tags:["Drama"],description:"A sufficiently detailed test description.",coverUrl:"/cover.jpg",cpsUrl:"https://reelslink.com/cps/example",episodes:[{episodeNumber:1,videoUrl:"https://media.example.test/e1.mp4"}]});expect(parsed.slug).toBe("test-drama")});it("rejects non-ReelShort and insecure destinations",()=>{const base={title:"Test Drama",slug:"test-drama",publicCode:"1234",promoCode:"1234",language:"en",tags:[],description:"A sufficiently detailed test description.",coverUrl:"/cover.jpg",episodes:[{episodeNumber:1,videoUrl:"https://media.example.test/e1.mp4"}]};expect(()=>dramaDraftSchema.parse({...base,cpsUrl:"https://evil.test/cps/x"})).toThrow();expect(()=>dramaDraftSchema.parse({...base,cpsUrl:"http://reelslink.com/cps/x"})).toThrow()})});
